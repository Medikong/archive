get:
  tags:
    - observability
  summary: Prometheus metrics.
  description: Exposes interest-service HTTP, dedup gate, and counter update metrics in Prometheus text exposition format.
  operationId: getMetrics
  security: []
  responses:
    "200":
      description: Prometheus text exposition format.
      content:
        text/plain:
          schema:
            type: string
