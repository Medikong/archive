get:
  tags:
    - operational
  summary: Service process health.
  description: Checks whether the interest-service process is alive. External dependencies are not checked here.
  operationId: getHealthz
  security: []
  responses:
    "200":
      description: Service process is alive.
      content:
        application/json:
          schema:
            $ref: ../components/schemas.yaml#/HealthResponse
