# Analysis

회사별 사례로 묶이지 않는 일반 구현 패턴이나, 여러 자료를 주제별로 다시 정리하는 영역이다.

## 읽는 기준

- `원문에서 확인한 내용`은 공개 자료에서 직접 확인한 사실만 담는다.
- `DropMong 적용 아이디어`는 공개 자료를 바탕으로 DropMong에 맞게 해석한 설계안이다.
- `확인 필요`는 공개 자료만으로 단정할 수 없는 내부 구현 또는 운영 정책이다.

## 분석 문서

| 문서 | 질문 | 핵심 결론 |
| --- | --- | --- |
| [01-view-count-dedup.md](01-view-count-dedup.md) | 조회수 중복 집계는 어떻게 방지하는가? | Redis `SETNX`/`EX` 기반 시간 윈도우 dedup이 일반적이며, 로그인/비로그인을 구분해 키를 관리하는 패턴이 흔하다. |
| [02-realtime-ranking-score-design.md](02-realtime-ranking-score-design.md) | 실시간 인기 점수는 절대량/로그스케일/시간감쇠 중 무엇을 쓰는가? | Reddit(로그 스케일+상대적 밀림), Hacker News(거듭제곱 감쇠), 네이버/다음(평균 대비 증가율), KREAM(다중 신호+기간 분리)이 각기 다른 전략을 쓰며, DropMong은 오픈 전엔 로그 스케일, 오픈 후엔 거듭제곱 감쇠를 검토할 수 있다. |
| [03-view-count-measurement-methods.md](03-view-count-measurement-methods.md) | "조회/관심"을 무엇으로 정의하고 측정하는가? | YouTube는 체류시간 기준으로 조회를 정의하고, 무신사/크림은 여러 신호를 조합한다. DropMong은 1차 스코프에서 "진입=조회"로 단순하게 가되 트레이드오프를 열어둔다. |

## 작성 기준

- 원문 링크와 저장 위치를 함께 남긴다.
- 특정 기업/개인 블로그 사례 하나만으로 일반화하지 않는다.
- 이번 조사는 검색 스니펫 기준이라, 원문 전체를 확인하지 못한 부분은 명시한다.
